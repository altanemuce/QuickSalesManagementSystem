@using QSMS.Application.Features.DTOs.Role;
@model GetByIdAppRoleDto
@{
    ViewData["Title"] = "Update";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
            <div class="col-md-6 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Default form</h4>
                        <p class="card-description">
                            Basic form layout
                        </p>
                        <form asp-controller="Role" asp-action="Update" class="forms-sample" method="post">
                            <input type="number" asp-for="Id" value="@Model.Id" style="display:none" />
                            <div class="form-group">
                                <label asp-for="RoleName" for="exampleInputUsername1">Rol Adı</label>
                                <input type="text" asp-for="RoleName" class="form-control" id="exampleInputUsername1" value="@Model.RoleName">
                            </div>

                            <button type="submit" class="btn btn-primary me-2">Rol Güncelle</button>

                        </form>
                    </div>
                </div>
            </div>

        </div>
    </div>
</div>

